{
	"name": "@js_node_chat/build",
	"scripts": {
		"runner": "node -r esbuild-runner/register ./index.ts",
		"build": "yarn runner build",
		"run": "yarn runner run",
		"destroy": "yarn runner destroy",
		"destroy-all": "yarn runner destroy-all",
		"hot-reload": "yarn runner hot-reload "
	},
	"dependencies": {
		"@js_node_chat/server": "workspace:*",
		"dotenv": "16.3.1",
		"esbuild": "0.19.10",
		"esbuild-runner": "2.2.2",
		"nodemon": "3.0.2"
	},
	"devDependencies": {
		"@types/node": "20.10.5",
		"@types/nodemon": "1.19.6"
	}
}
